/**
 * NOTA:
 * Estos métodos cuelgan del objeto global 'window'
 */
console.log(window);
// window.alert("Esto es una alerta!");
// window.confirm("Esta ventana requiere una confirmación.");
// window.prompt("Hola, esto es un prompt");
let alerta = alert("Esto es una alerta!");
let confirmacion = confirm("Esta ventana requiere una confirmación.");
let aviso = prompt("Hola, esto es un prompt");

console.log(alerta);
console.log(confirmacion);
console.log(aviso);